
import React from 'react'

const Welcome = props => (
  <div className="middle full">
    <div className="text-big text-center">
      Welcome to the HopeBox mobile phone simulator.  This will simulate the
      interaction between the different users of the platform without the need
      to have the hardware running locally.
    </div>
  </div>
)

export default Welcome
